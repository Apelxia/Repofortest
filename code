  if state = start then
      if newcoin = '1' then
        next_state <= coin_insert;
      elsif x = '1' then
        next_state <= start_acc;
      end if;
    elsif state = coin_insert then
      if coindrop = '1' then
        next_state <= can_drop;
      end if;
