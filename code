 SYNC_PROC: process(clk, reset)
  begin
    if reset = '1' then
      count_i <= ustart;  -- Reset counter to start value
    elsif rising_edge(clk) then
      if count_i = ufinish then
        count_i <= ustart;  -- Wrap around from finish to start
      else
        count_i <= count_i + 1;  -- Increment counter
      end if;
    end if;
  end process SYNC_PROC;
end rtl;
S220069582@13025PodB:~/Digital Design/ee2did-coursework-2023-Apelxia$ make counter.check
ghdl -a --std=08 --workdir=tmp/ counter.vhd
counter.vhd:31:41:error: unexpected token '=' in a primary
counter.vhd:35:5:error: 'begin' not allowed within statements
